export enum WorkDuration {
  Medium = 'medium',
  ShortTerm = 'short-term',
  LongTerm = 'long-term',
  Permanent = 'permanent',
}

export enum Shift {
  Day = 'day',
  Night = 'night',
  Both = 'both',
}
